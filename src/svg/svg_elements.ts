// THIS FILE IS AUTO-GENERATED, DO NOT MODIFY.
// See ./scripts/svg-element-generator.mts to make changes.
import type { SVGElement } from "./index.ts";
import type { CHILDREN_MAP } from "./svg_children.ts";

interface AElement extends SVGElement<"a", CHILDREN_MAP["a"]> {}

interface AnimateElement extends SVGElement<"animate", CHILDREN_MAP["animate"]> {}

interface AnimateMotionElement extends SVGElement<"animateMotion", CHILDREN_MAP["animateMotion"]> {}

interface AnimateTransformElement extends SVGElement<"animateTransform", CHILDREN_MAP["animateTransform"]> {}

interface AudioElement extends SVGElement<"audio", CHILDREN_MAP["audio"]> {}

interface CanvasElement extends SVGElement<"canvas", CHILDREN_MAP["canvas"]> {}

interface CircleElement extends SVGElement<"circle", CHILDREN_MAP["circle"]> {}

interface ClipPathElement extends SVGElement<"clipPath", CHILDREN_MAP["clipPath"]> {}

interface DefsElement extends SVGElement<"defs", CHILDREN_MAP["defs"]> {}

interface DescElement extends SVGElement<"desc", CHILDREN_MAP["desc"]> {}

interface DiscardElement extends SVGElement<"discard", CHILDREN_MAP["discard"]> {}

interface EllipseElement extends SVGElement<"ellipse", CHILDREN_MAP["ellipse"]> {}

interface FeBlendElement extends SVGElement<"feBlend", CHILDREN_MAP["feBlend"]> {}

interface FeColorMatrixElement extends SVGElement<"feColorMatrix", CHILDREN_MAP["feColorMatrix"]> {}

interface FeComponentTransferElement extends SVGElement<"feComponentTransfer", CHILDREN_MAP["feComponentTransfer"]> {}

interface FeCompositeElement extends SVGElement<"feComposite", CHILDREN_MAP["feComposite"]> {}

interface FeConvolveMatrixElement extends SVGElement<"feConvolveMatrix", CHILDREN_MAP["feConvolveMatrix"]> {}

interface FeDiffuseLightingElement extends SVGElement<"feDiffuseLighting", CHILDREN_MAP["feDiffuseLighting"]> {}

interface FeDisplacementMapElement extends SVGElement<"feDisplacementMap", CHILDREN_MAP["feDisplacementMap"]> {}

interface FeDistantLightElement extends SVGElement<"feDistantLight", CHILDREN_MAP["feDistantLight"]> {}

interface FeDropShadowElement extends SVGElement<"feDropShadow", CHILDREN_MAP["feDropShadow"]> {}

interface FeFloodElement extends SVGElement<"feFlood", CHILDREN_MAP["feFlood"]> {}

interface FeFuncAElement extends SVGElement<"feFuncA", CHILDREN_MAP["feFuncA"]> {}

interface FeFuncBElement extends SVGElement<"feFuncB", CHILDREN_MAP["feFuncB"]> {}

interface FeFuncGElement extends SVGElement<"feFuncG", CHILDREN_MAP["feFuncG"]> {}

interface FeFuncRElement extends SVGElement<"feFuncR", CHILDREN_MAP["feFuncR"]> {}

interface FeGaussianBlurElement extends SVGElement<"feGaussianBlur", CHILDREN_MAP["feGaussianBlur"]> {}

interface FeImageElement extends SVGElement<"feImage", CHILDREN_MAP["feImage"]> {}

interface FeMergeElement extends SVGElement<"feMerge", CHILDREN_MAP["feMerge"]> {}

interface FeMergeNodeElement extends SVGElement<"feMergeNode", CHILDREN_MAP["feMergeNode"]> {}

interface FeMorphologyElement extends SVGElement<"feMorphology", CHILDREN_MAP["feMorphology"]> {}

interface FeOffsetElement extends SVGElement<"feOffset", CHILDREN_MAP["feOffset"]> {}

interface FePointLightElement extends SVGElement<"fePointLight", CHILDREN_MAP["fePointLight"]> {}

interface FeSpecularLightingElement extends SVGElement<"feSpecularLighting", CHILDREN_MAP["feSpecularLighting"]> {}

interface FeSpotLightElement extends SVGElement<"feSpotLight", CHILDREN_MAP["feSpotLight"]> {}

interface FeTileElement extends SVGElement<"feTile", CHILDREN_MAP["feTile"]> {}

interface FeTurbulenceElement extends SVGElement<"feTurbulence", CHILDREN_MAP["feTurbulence"]> {}

interface FilterElement extends SVGElement<"filter", CHILDREN_MAP["filter"]> {}

interface ForeignObjectElement extends SVGElement<"foreignObject", CHILDREN_MAP["foreignObject"]> {}

interface GElement extends SVGElement<"g", CHILDREN_MAP["g"]> {}

interface IframeElement extends SVGElement<"iframe", CHILDREN_MAP["iframe"]> {}

interface ImageElement extends SVGElement<"image", CHILDREN_MAP["image"]> {}

interface LineElement extends SVGElement<"line", CHILDREN_MAP["line"]> {}

interface LinearGradientElement extends SVGElement<"linearGradient", CHILDREN_MAP["linearGradient"]> {}

interface MarkerElement extends SVGElement<"marker", CHILDREN_MAP["marker"]> {}

interface MaskElement extends SVGElement<"mask", CHILDREN_MAP["mask"]> {}

interface MetadataElement extends SVGElement<"metadata", CHILDREN_MAP["metadata"]> {}

interface MpathElement extends SVGElement<"mpath", CHILDREN_MAP["mpath"]> {}

interface PathElement extends SVGElement<"path", CHILDREN_MAP["path"]> {}

interface PatternElement extends SVGElement<"pattern", CHILDREN_MAP["pattern"]> {}

interface PolygonElement extends SVGElement<"polygon", CHILDREN_MAP["polygon"]> {}

interface PolylineElement extends SVGElement<"polyline", CHILDREN_MAP["polyline"]> {}

interface RadialGradientElement extends SVGElement<"radialGradient", CHILDREN_MAP["radialGradient"]> {}

interface RectElement extends SVGElement<"rect", CHILDREN_MAP["rect"]> {}

interface ScriptElement extends SVGElement<"script", CHILDREN_MAP["script"]> {}

interface SetElement extends SVGElement<"set", CHILDREN_MAP["set"]> {}

interface StopElement extends SVGElement<"stop", CHILDREN_MAP["stop"]> {}

interface StyleElement extends SVGElement<"style", CHILDREN_MAP["style"]> {}

interface SvgElement extends SVGElement<"svg", CHILDREN_MAP["svg"]> {}

interface SwitchElement extends SVGElement<"switch", CHILDREN_MAP["switch"]> {}

interface SymbolElement extends SVGElement<"symbol", CHILDREN_MAP["symbol"]> {}

interface TextElement extends SVGElement<"text", CHILDREN_MAP["text"]> {}

interface TextPathElement extends SVGElement<"textPath", CHILDREN_MAP["textPath"]> {}

interface TitleElement extends SVGElement<"title", CHILDREN_MAP["title"]> {}

interface TspanElement extends SVGElement<"tspan", CHILDREN_MAP["tspan"]> {}

interface UnknownElement extends SVGElement<"unknown", CHILDREN_MAP["unknown"]> {}

interface UseElement extends SVGElement<"use", CHILDREN_MAP["use"]> {}

interface VideoElement extends SVGElement<"video", CHILDREN_MAP["video"]> {}

interface ViewElement extends SVGElement<"view", CHILDREN_MAP["view"]> {}

/** A type-safe representation of all SVG elements. */
export type ELEMENT_MAP = {

  /** A not-so type-safe representation of the `<a>` element. */
  a: AElement;

  /** A not-so type-safe representation of the `<animate>` element. */
  animate: AnimateElement;

  /** A not-so type-safe representation of the `<animateMotion>` element. */
  animateMotion: AnimateMotionElement;

  /** A not-so type-safe representation of the `<animateTransform>` element. */
  animateTransform: AnimateTransformElement;

  /** A not-so type-safe representation of the `<audio>` element. */
  audio: AudioElement;

  /** A not-so type-safe representation of the `<canvas>` element. */
  canvas: CanvasElement;

  /** A not-so type-safe representation of the `<circle>` element. */
  circle: CircleElement;

  /** A not-so type-safe representation of the `<clipPath>` element. */
  clipPath: ClipPathElement;

  /** A not-so type-safe representation of the `<defs>` element. */
  defs: DefsElement;

  /** A not-so type-safe representation of the `<desc>` element. */
  desc: DescElement;

  /** A not-so type-safe representation of the `<discard>` element. */
  discard: DiscardElement;

  /** A not-so type-safe representation of the `<ellipse>` element. */
  ellipse: EllipseElement;

  /** A not-so type-safe representation of the `<feBlend>` element. */
  feBlend: FeBlendElement;

  /** A not-so type-safe representation of the `<feColorMatrix>` element. */
  feColorMatrix: FeColorMatrixElement;

  /** A not-so type-safe representation of the `<feComponentTransfer>` element. */
  feComponentTransfer: FeComponentTransferElement;

  /** A not-so type-safe representation of the `<feComposite>` element. */
  feComposite: FeCompositeElement;

  /** A not-so type-safe representation of the `<feConvolveMatrix>` element. */
  feConvolveMatrix: FeConvolveMatrixElement;

  /** A not-so type-safe representation of the `<feDiffuseLighting>` element. */
  feDiffuseLighting: FeDiffuseLightingElement;

  /** A not-so type-safe representation of the `<feDisplacementMap>` element. */
  feDisplacementMap: FeDisplacementMapElement;

  /** A not-so type-safe representation of the `<feDistantLight>` element. */
  feDistantLight: FeDistantLightElement;

  /** A not-so type-safe representation of the `<feDropShadow>` element. */
  feDropShadow: FeDropShadowElement;

  /** A not-so type-safe representation of the `<feFlood>` element. */
  feFlood: FeFloodElement;

  /** A not-so type-safe representation of the `<feFuncA>` element. */
  feFuncA: FeFuncAElement;

  /** A not-so type-safe representation of the `<feFuncB>` element. */
  feFuncB: FeFuncBElement;

  /** A not-so type-safe representation of the `<feFuncG>` element. */
  feFuncG: FeFuncGElement;

  /** A not-so type-safe representation of the `<feFuncR>` element. */
  feFuncR: FeFuncRElement;

  /** A not-so type-safe representation of the `<feGaussianBlur>` element. */
  feGaussianBlur: FeGaussianBlurElement;

  /** A not-so type-safe representation of the `<feImage>` element. */
  feImage: FeImageElement;

  /** A not-so type-safe representation of the `<feMerge>` element. */
  feMerge: FeMergeElement;

  /** A not-so type-safe representation of the `<feMergeNode>` element. */
  feMergeNode: FeMergeNodeElement;

  /** A not-so type-safe representation of the `<feMorphology>` element. */
  feMorphology: FeMorphologyElement;

  /** A not-so type-safe representation of the `<feOffset>` element. */
  feOffset: FeOffsetElement;

  /** A not-so type-safe representation of the `<fePointLight>` element. */
  fePointLight: FePointLightElement;

  /** A not-so type-safe representation of the `<feSpecularLighting>` element. */
  feSpecularLighting: FeSpecularLightingElement;

  /** A not-so type-safe representation of the `<feSpotLight>` element. */
  feSpotLight: FeSpotLightElement;

  /** A not-so type-safe representation of the `<feTile>` element. */
  feTile: FeTileElement;

  /** A not-so type-safe representation of the `<feTurbulence>` element. */
  feTurbulence: FeTurbulenceElement;

  /** A not-so type-safe representation of the `<filter>` element. */
  filter: FilterElement;

  /** A not-so type-safe representation of the `<foreignObject>` element. */
  foreignObject: ForeignObjectElement;

  /** A not-so type-safe representation of the `<g>` element. */
  g: GElement;

  /** A not-so type-safe representation of the `<iframe>` element. */
  iframe: IframeElement;

  /** A not-so type-safe representation of the `<image>` element. */
  image: ImageElement;

  /** A not-so type-safe representation of the `<line>` element. */
  line: LineElement;

  /** A not-so type-safe representation of the `<linearGradient>` element. */
  linearGradient: LinearGradientElement;

  /** A not-so type-safe representation of the `<marker>` element. */
  marker: MarkerElement;

  /** A not-so type-safe representation of the `<mask>` element. */
  mask: MaskElement;

  /** A not-so type-safe representation of the `<metadata>` element. */
  metadata: MetadataElement;

  /** A not-so type-safe representation of the `<mpath>` element. */
  mpath: MpathElement;

  /** A not-so type-safe representation of the `<path>` element. */
  path: PathElement;

  /** A not-so type-safe representation of the `<pattern>` element. */
  pattern: PatternElement;

  /** A not-so type-safe representation of the `<polygon>` element. */
  polygon: PolygonElement;

  /** A not-so type-safe representation of the `<polyline>` element. */
  polyline: PolylineElement;

  /** A not-so type-safe representation of the `<radialGradient>` element. */
  radialGradient: RadialGradientElement;

  /** A not-so type-safe representation of the `<rect>` element. */
  rect: RectElement;

  /** A not-so type-safe representation of the `<script>` element. */
  script: ScriptElement;

  /** A not-so type-safe representation of the `<set>` element. */
  set: SetElement;

  /** A not-so type-safe representation of the `<stop>` element. */
  stop: StopElement;

  /** A not-so type-safe representation of the `<style>` element. */
  style: StyleElement;

  /** A not-so type-safe representation of the `<svg>` element. */
  svg: SvgElement;

  /** A not-so type-safe representation of the `<switch>` element. */
  switch: SwitchElement;

  /** A not-so type-safe representation of the `<symbol>` element. */
  symbol: SymbolElement;

  /** A not-so type-safe representation of the `<text>` element. */
  text: TextElement;

  /** A not-so type-safe representation of the `<textPath>` element. */
  textPath: TextPathElement;

  /** A not-so type-safe representation of the `<title>` element. */
  title: TitleElement;

  /** A not-so type-safe representation of the `<tspan>` element. */
  tspan: TspanElement;

  /** A not-so type-safe representation of the `<unknown>` element. */
  unknown: UnknownElement;

  /** A not-so type-safe representation of the `<use>` element. */
  use: UseElement;

  /** A not-so type-safe representation of the `<video>` element. */
  video: VideoElement;

  /** A not-so type-safe representation of the `<view>` element. */
  view: ViewElement;
}